<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BrowserX.Settings.System.SystemMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrowserX.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <ScrollView>
        <StackPanel
            Spacing="{StaticResource NavigationViewFrameSpacing}"
            Margin="{StaticResource NavigationViewFrameMargin}">
            <controls:SettingsCard
                Header="系统"
                Description="调整背景模式和图形加速等系统设置"
                IsClickEnabled="True"
                Click="JumpToSystemItem">
            </controls:SettingsCard>

            <controls:SettingsCard
                Header="性能"
                Description="调整性能设置，如节能模式和睡眠选项卡"
                IsClickEnabled="True"
                Click="JumpToPerformanceItem">
            </controls:SettingsCard>
            
            <controls:SettingsCard
                Header="使用 F12 键打开开发人员工具">
                <ToggleSwitch />
            </controls:SettingsCard>
            
            <controls:SettingsCard
                Header="管理打印机"
                Click="OpenSettingsPrinter"
                IsClickEnabled="True"
                HeaderIcon="{ui:FontIcon Glyph=&#xe749;}"
                ActionIcon="{ui:FontIcon Glyph=&#xE8A7;}">
            </controls:SettingsCard>
        </StackPanel>
    </ScrollView>
</Page>
